export interface IHeader {
    id: string;
    backButton: string;
    songName: string;
    options: string;
}

export interface IMusicTracks {
    id: string;
    songName: string;
    image: string;
}